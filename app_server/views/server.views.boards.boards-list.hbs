<div class="row">
  <div class="col-xs-12">
    <div class="row">
      <h3 class="text-center">{{pageHeader.title}}</h3>
      {{#each boards}}
        <div class="list-group">
          <div class="col-xs-12 list-group-item">
            <h4><a href="/boards/{{_id}}/{{name}}">{{name}}</a></h4>
          </div>
        </div>
      {{/each}}
    </div>
  </div>
</div>

{{!-- Pagination --}}
{{> server.views.partials.pagination }}
